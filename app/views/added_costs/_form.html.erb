<%= form_for(added_cost) do |f| %>
  <% if added_cost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(added_cost.errors.count, "error") %> prohibited this added_cost from being saved:</h2>

      <ul>
      <% added_cost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cost_date %>
    <%= f.datetime_select :cost_date %>
  </div>

  <div class="field">
    <%= f.label :cost %>
    <%= f.text_field :cost %>
  </div>

  <div class="field">
    <%= f.label :billed %>
    <%= f.check_box :billed %>
  </div>

  <div class="field">
    <%= f.label :labor_cost_total %>
    <%= f.text_field :labor_cost_total %>
  </div>

  <div class="field">
    <%= f.label :total %>
    <%= f.text_field :total %>
  </div>

  <div class="field">
    <%= f.label :method_of_payment %>
    <%= f.text_field :method_of_payment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
