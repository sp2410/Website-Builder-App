<%= form_for(added_cost_vendor) do |f| %>
  <% if added_cost_vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(added_cost_vendor.errors.count, "error") %> prohibited this added_cost_vendor from being saved:</h2>

      <ul>
      <% added_cost_vendor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :contact %>
    <%= f.text_field :contact %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
