<%= form_for(previous_owner) do |f| %>
  <% if previous_owner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(previous_owner.errors.count, "error") %> prohibited this previous_owner from being saved:</h2>

      <ul>
      <% previous_owner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :street_address %>
    <%= f.text_field :street_address %>
  </div>

  <div class="field">
    <%= f.label :zip %>
    <%= f.text_field :zip %>
  </div>

  <div class="field">
    <%= f.label :city %>
    <%= f.text_field :city %>
  </div>

  <div class="field">
    <%= f.label :state %>
    <%= f.text_field :state %>
  </div>

  <div class="field">
    <%= f.label :phone %>
    <%= f.text_field :phone %>
  </div>

  <div class="field">
    <%= f.label :used_vehicle_for %>
    <%= f.text_field :used_vehicle_for %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
